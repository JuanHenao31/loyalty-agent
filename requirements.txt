# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Validation & env
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0

# HTTP client (loyalty core + channel APIs)
httpx>=0.27.0

# Database / persistence
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
psycopg[binary]>=3.2.0
alembic>=1.13.0

# LangChain / LangGraph agent stack
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.40
langgraph-checkpoint-postgres>=2.0.0

# OpenAI SDK (used transitively by langchain-openai; pinned for clarity)
openai>=1.40.0

# Crypto (Fernet for refresh tokens)
cryptography>=42.0.0

# Dev tools
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0
